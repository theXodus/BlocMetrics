<%= form_for @registeredApplication do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :url %>
  <%= f.text_field :url, placeholder: "include http:// or https://"%>
  <%= f.submit "Save" %>
<% end %>
